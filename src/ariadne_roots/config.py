# config.py
# Global configuration for scaling factors, # Allows to store the scale set by the user to generate correct scaling

length_scale_factor = 1.0  # Default: 1 pixel = 1.0 unit
length_scale_unit = "px"   # Default unit
